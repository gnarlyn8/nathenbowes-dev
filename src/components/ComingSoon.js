import React from "react";

const ComingSoon = () => {
  return (
    <div
      style={{
        fontFamily: "system-ui",
        height: "100vh",
        display: "grid",
        placeItems: "center",
        textAlign: "center",
      }}
    >
      <div>
        <h1>🚧 Coming Soon</h1>
        <p>Thanks for stopping by — the site’s still being built.</p>
      </div>
    </div>
  );
};

export default ComingSoon;
